<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <android.support.design.widget.AppBarLayout
        android:id="@+id/app_bar_fuli"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <!--Toolbar-->
        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar_fuli"
            android:layout_width="match_parent"
            android:layout_height="@dimen/tool_bar_height"
            android:background="?attr/colorPrimary"
            android:minHeight="@dimen/tool_bar_height"
            app:popupTheme="@style/MyOptionMenuStyle"
            app:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar" />
    </android.support.design.widget.AppBarLayout>

    <com.jude.easyrecyclerview.EasyRecyclerView
        android:id="@+id/recycler_fuli"
        app:scrollbarStyle="insideOverlay"
        app:layout_progress="@layout/progress_layout"
        android:layout_below="@id/app_bar_fuli"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <com.zurich.mobile.widget.HintView
        android:id="@+id/hint_fuli_hint"
        android:layout_below="@id/app_bar_fuli"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
</RelativeLayout>